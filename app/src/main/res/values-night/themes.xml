<resources>
    <!-- Base application theme. -->
    <style name="Theme.StreamingApp" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/dark_gray</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <!-- translucent navigation bar -->
        <item name="android:navigationBarColor">?attr/colorPrimary</item>
        <item name="android:navigationIcon">@color/white</item>
    </style>
    <!-- custom alert dialog -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@drawable/shape_alert_dialog_box</item>
    </style>
    <!-- splash screen -->
    <style name="splashScreenTheme" parent="Theme.AppCompat.DayNight.NoActionBar"/>
</resources>